Other versions of the timer namespace/class.
In particular contains `main_cpp14.cpp` that can be compiled with c++14.
c++14 version is called with flags (unlike the main version, that is called with variadic template options).
